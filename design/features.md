使用逻辑（结合好好+flomo 的输入）：

log -> 整理 -> search/recap -> AI 洞察


1. 下拉，出现一些诗词之类的，“诗意”；或者 AI 洞察；或者历史回顾
2. 搜索做成 1 个按钮就行，点击出现单独的页面
3. archived 的 topics，设置有一个入口进去；同时提示，tab 右滑，也能打开这个入口。进去后有分组、有 filter（按时间）
4. topics 后面可以支持分组（但或许不过刚需，因为活动的 topics 不多，记录的才可能多）
